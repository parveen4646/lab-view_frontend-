# Optional: only needed if the frontend is NOT served behind the same origin
# as the backend (e.g. separate hosts in production). In local dev the Vite
# proxy in vite.config.js handles /api requests automatically.
VITE_API_URL=
